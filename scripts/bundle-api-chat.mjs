import { build } from 'esbuild';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const root = join(dirname(fileURLToPath(import.meta.url)), '..');

await build({
  absWorkingDir: root,
  entryPoints: ['lib/chat/vercelEntry.ts'],
  outfile: 'api/chat.js',
  bundle: true,
  platform: 'node',
  format: 'esm',
  packages: 'external',
  target: 'node20',
  legalComments: 'none',
  sourcemap: false,
  keepNames: true,
  logLevel: 'info',
  banner: {
    js: '/* Generated by scripts/bundle-api-chat.mjs — source: lib/chat/vercelEntry.ts. Do not edit. */\n',
  },
});
